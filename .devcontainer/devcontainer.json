{
  "name": "RAG OpenAI Dev Container",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "workspaceFolder": "/app",
  "forwardPorts": [8501],
  "postCreateCommand": "python -m venv ./.venv && export PATH='/app/.venv/bin:$PATH' && pip install --upgrade pip && pip install -r requirements.txt",
}
